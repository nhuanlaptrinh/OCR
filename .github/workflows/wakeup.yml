name: Wake Up Streamlit App

on:
  schedule:
    # Chạy sau mỗi 15 phút
    - cron: '*/15 * * * *'

jobs:
  wake-up:
    runs-on: ubuntu-latest
    steps:
      - name: Send request to app
        run: curl -s https://vqgfnjwpybwjtqrh6senci.streamlit.app/ > /dev/null
